# 新手路径检查 · v0.8.6

测试人物：会用 Excel 或 Origin，第一次接触 AI 技能，不懂终端，只想把电池数据画成论文图。本轮做了路径静态检查、脚本测试与本地浏览器走查，**尚未招募真人独立试用**。

1. 首页：保留既有口号、六配色预览和原有样图入口。
2. 理解：Why 页说明与现有工具的关系，也说明何时直接用 Origin 更合适。
3. 安装：上手页只展示三个阶段；“不确定”先诊断，不直接去安装新软件；Codex 优先，其他软件按需展开。
4. 示例：全电池或 Li‖Cu 各下载一个 ZIP，包内两份 CSV 与 README，复制对应任务句。
5. 输出：任务句要求可编辑 SVG，说明虚构数据并核对原文件。

本地浏览器已走通“不确定 → 诊断 → 找到软件 → Codex → Windows → 安装说明 → 演示包”。该走查只验证页面分支和下载入口，没有代替一个新手完成真实安装。

**安装路径状态：**Windows 安装脚本及其冲突处理可以独立测试。Codex 辅助安装尚未完成三次“从用户提示到新任务可用”的完整实机验证，暂不将其写作默认安装方式。PowerShell 步骤保留并改成可照做的地址栏操作；后续实机验证后再调整默认路线。

待真实测试：至少让新手无口头指导走完“访问 → 理解 → 安装 → 下载 → SVG”，记录停顿、误解、失败和时长。
